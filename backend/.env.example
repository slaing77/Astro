# Example environment config for the Astro Bot backend (SQLite version)

# Local SQLite database file path
DATABASE_URL=sqlite:///./astrobot.db

# Secret key for JWT token signing
SECRET_KEY=replace-with-your-own-secure-secret
